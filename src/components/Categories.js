import styled from "styled-components";
import { BlockHeader,
         Button,
         Text } from "../basic"; 

const BlockContainer = styled.div`
`

export const Categories = () => {
    return(<>
    </>)
}